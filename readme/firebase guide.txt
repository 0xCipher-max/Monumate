just make a file name as "firebase.config" in the root folder and add basic firebase detials generated by you. All the database will be created during running od application.
Just sites need to be added manually and its structure is in attached screenshot.
make guard (who will scan the qr at entry) users with @gaurd.com
make normal users with anny domain.